version: '2.2'
services:
  mongo:
    image: mongo
    container_name: mongobotdb
  wheremymovie:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: botapp
    depends_on:
      - mongo
    links:
      - mongo
